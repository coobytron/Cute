# Thumbnails

Store production thumbnail crops here when the source artwork cannot serve directly as its own preview.

Thumbnails are square, centered consistently, readable at approximately 96 px, and contain no baked-in labels or selection chrome. Every thumbnail path is declared in `assets/manifest.json` and validated before release.
