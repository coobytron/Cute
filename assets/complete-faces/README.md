# Complete faces

Production-ready precomposed transparent PNG or SVG character faces belong here.

The five current hero assets remain in `previews/` during the manifest migration. New files should use the canonical 1000 × 1000 coordinate system, stable manifest IDs, consistent scale, declared export bounds, and separate thumbnails when needed.
